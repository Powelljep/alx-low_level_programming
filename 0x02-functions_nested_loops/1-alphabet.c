#include "main.h"
/**
 * print_alphabet - prints alphabets in lowercase
 *
 * Description: check description
 * Return: returs void
 */

voi print_alphabet(void)
{
	char a = NULL;
	
	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
	return (0);
}
