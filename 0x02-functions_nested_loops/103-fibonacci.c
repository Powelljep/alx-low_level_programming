#include <stdio.h>
/**
 * sum_even_fibonacci - prints sum of fibonacci sequence
 *
 * Description: finds and prints the sum of the 
 * even-valued terms, followed by a new line
 * Return: returns void
 */
void sum_even_fibonacci(void)
{
	int sum, i, even_fibo;

	for (i = 1; i <= 4000000; i++)
	{
		even= i
	}
}
