#include <stdio.h>
/**
 * _atoi -  convert a string to an integer.
 * @: string to convert to interger
 *
 * Description: a function that convert a string to an integer.
 * Return: returns int.
 */
int _atoi(char *s)
{
}
