#include "main.h"
/**
 * swap_int - swaps the values of two integers
 * @a: first value to be swapped with the second value.
 * @b: second value to be swapped with the first value.
 *
 * Description: a function that swaps the values of two integers.
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int swap = *a;
	*a = *b;
	*b = swap
}
